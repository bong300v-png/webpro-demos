---
description: Auto commit and push webpro-demos to GitHub (Cloudflare Pages auto-deploy)
---

// turbo-all

1. Navigate to webpro-demos directory
2. Stage all changes: `git add -A`
3. Commit with descriptive message: `git commit -m "<message>"`
4. Push to remote: `git push`
